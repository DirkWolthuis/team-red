import React from "react";
import styled from "styled-components";

const StyledLists = styled.div`
  width: 50%;
`;

const Lists = () => {
  return <StyledLists>xxx</StyledLists>;
};

export default Lists;
